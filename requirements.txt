# ---- Web server ----
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6
httpx>=0.25.0

# ---- Document extraction ----
PyPDF2>=3.0.0
python-docx>=1.0.0
python-pptx>=0.6.23
openpyxl>=3.1.0
Pillow>=10.0.0

# ---- OCR for image-based slides (bundled, no Tesseract/Poppler needed) ----
rapidocr-onnxruntime>=1.3.0   # ONNX OCR engine, ships its own models
PyMuPDF>=1.23.0               # rasterize image-only / scanned PDF pages
pytesseract>=0.3.10           # optional fallback if Tesseract is installed

# ---- Local AI (in-process llama.cpp: Qwen2.5-1.5B-Instruct + nomic-embed-text) ----
llama-cpp-python>=0.3.2      # runs the bundled GGUF models on-device, no Ollama
numpy>=1.24.0
sympy>=1.12                  # deterministic calculation-question solving

# ---- Desktop packaging (build-time only) ----
pyinstaller>=6.3.0           # freezes the backend into the bundled sidecar binary
