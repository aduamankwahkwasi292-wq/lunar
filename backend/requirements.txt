# ---- Web server ----
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6
httpx>=0.25.0

# ---- Document extraction ----
PyPDF2>=3.0.0
python-docx>=1.0.0
python-pptx>=0.6.23
openpyxl>=3.1.0
Pillow>=10.0.0

# ---- OCR for image-based slides (bundled, no Tesseract/Poppler needed) ----
rapidocr-onnxruntime>=1.3.0   # ONNX OCR engine, ships its own models
PyMuPDF>=1.23.0               # rasterize image-only / scanned PDF pages
pytesseract>=0.3.10           # optional fallback if Tesseract is installed

# ---- Local AI (RAG over Ollama: qwen3:1.7b + nomic-embed-text) ----
numpy>=1.24.0
sympy>=1.12                  # deterministic calculation-question solving

# ---- Local voice: speech-to-text + text-to-speech ----
faster-whisper>=1.0.0
soundfile>=0.12.1
piper-tts>=1.2.0
pyttsx3>=2.90
